#include "main.h"

/**
 * _isdigit - checks for digit 0-9
 * @c: value evaluated
 *
 * return 1 if c is a digit, return 0 if not
 */

int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
		return (1);

	else
	{

	return (0);
	}
}
