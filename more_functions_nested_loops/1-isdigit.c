#include "main.h"

/**
 * _isdigit - checks for digit 0-9
 * @c: value evaluated
 *
 * Return: 1 - if the character is a digit, Return: 0 - otherwise
 */

int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
		return (1);

	else
	{

	return (0);
	}
}
