#include "main.h"

/**
 * _isdigit - checks for digit 0-9
 * @c: value evaluated
 *
 * Return 1 if is digit otherwise return 0
 */

int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
		return (1);

	else
	{

	return (0);
	}
}
